# REMOTE SENSING

- Rainfall: Open-Meteo forecast (LIVE, verified) + ERA5 archive (OBSERVED
  blend) for history. Never called real-time when archived.
- Soil: ERA5-Land reanalysis = MODELED. Never sensor data.
- Satellite: ASF discovery = OBSERVED metadata (10 S1 records: granule,
  time, beam, URL, md5). Imagery: 403 auth boundary PROVEN (Earthdata login
  required) → METADATA-ONLY status, quarantined DEMO path untouched.
- DEM: SRTM GL1 30m via OpenTopodata = OBSERVED (point profiles + 648-cell
  grids). Never "live terrain".
- Terminology enforced: LIVE (current feed) / ARCHIVE / MODELED / OBSERVED /
  SIMULATED / DEMO / UNAVAILABLE — see claim audit.
